services:
  angular-app:
    build: .
    container_name: angular-01_proyecto_inicial-v18
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Necesario para detectar cambios en Windows/WSL